apiVersion: v1
kind: Service
metadata:
  name: nlp-text-summarizer
  labels:
    app: nlp-text-summarizer
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
  selector:
    app: nlp-text-summarizer
